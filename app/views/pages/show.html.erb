Title: <%= @page.title %><p />

Backlinks:
<% @backlinks.each do |backlink| %>
  <%= link_to(backlink.title, :controller => "pages", :repository_id => @repository, :id => backlink, :action=>"show") %>
<% end %>
<p />

Link chain:
<% @link_chain[1..-1].each do |link_chain_page| %>
  <%= link_to(link_chain_page.title, :controller => "pages", :repository_id => @repository, :id => link_chain_page, :action=>"show") %>
<% end %>
<p />

